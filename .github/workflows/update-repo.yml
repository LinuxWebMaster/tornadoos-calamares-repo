name: TornadoOS Repo Auto-Update
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest
      options: --privileged
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          # Ensure .git folder is preserved
          persist-credentials: true

      - name: Debug After Checkout
        run: |
          echo "=== After Checkout ==="
          pwd
          ls -la
          ls -la .git 2>&1 | head -5 || echo ".git folder missing!"

      - name: Setup & Update
        run: |
          # 1. Install base dependencies
          pacman -Sy --noconfirm git base-devel sudo
          
          # 2. Build and install aurutils FROM AUR
          useradd -m builder
          echo "builder ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers.d/builder
          sudo -u builder git clone https://aur.archlinux.org/aurutils.git /tmp/aurutils
          sudo -u builder bash -c "cd /tmp/aurutils && makepkg -si --noconfirm"
          
          # 3. Add custom repo to pacman.conf
          mkdir -p "${{ github.workspace }}/x86_64"
          printf "\n[tornadoos-repo]\nSigLevel = Optional TrustAll\nServer = file://%s/x86_64\n" "${{ github.workspace }}" >> /etc/pacman.conf
          
          # 4. Setup builduser
          useradd -m builduser
          echo "builduser ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers.d/builduser
          
          # 5. Configure git safe directory
          git config --global --add safe.directory "${{ github.workspace }}"
          
          # 6. Change ownership of files ONLY (not .git folder)
          # Use a safer method that explicitly excludes .git
          for item in *; do
            if [ "$item" != ".git" ] && [ -e "$item" ]; then
              chown -R builduser:builduser "$item"
            fi
          done

      - name: Debug After Setup
        run: |
          echo "=== After Setup ==="
          ls -la
          ls -la .git 2>&1 | head -3 || echo ".git folder missing!"

      - name: Sync AUR Packages
        run: |
          sudo -u builduser git config --global --add safe.directory "${{ github.workspace }}"
          
          # Rebuild database from existing packages
          cd "${{ github.workspace }}/x86_64"
          sudo -u builduser repo-add tornadoos-repo.db.tar.gz *.pkg.tar.zst 2>/dev/null || true
          
          rm -f *.old

      - name: Push Updates
        run: |
          echo "=== Before Push ==="
          pwd
          ls -la
          
          # Verify .git exists
          if [ ! -d "${{ github.workspace }}/.git" ]; then
            echo "❌ ERROR: .git folder is missing!"
            exit 1
          fi
          
          cd "${{ github.workspace }}"
          
          # Ensure .git is accessible by root
          chmod -R u+rwX .git
          
          git config --global --add safe.directory "${{ github.workspace }}"
          git config --global user.name "TornadoOS Bot"
          git config --global user.email "ricki.clay@protonmail.com"
          
          git add x86_64/
          if git commit -m "Automated AUR Update $(date +%Y-%m-%d)"; then
            git push https://${{ secrets.TOKEN_NAME }}@github.com/LinuxWebMaster/tornadoos-repo.git HEAD:main
          else
            echo "✅ No changes to commit, skipping push."
          fi